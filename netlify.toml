[build]
  base    = "frontend" # Optional: If your frontend is in a subdirectory
  publish = "frontend/build" # The folder where your production build is located
  command = "npm run build" # The build command to create the production build

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 
